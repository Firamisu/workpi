# WorkPi - Pi digits Calculator

A simple React app that calculates π (Pi) up to 1,000,000 digits using a Web Worker for smooth, non-blocking performance.

## Setup

```bash
npm install
npm start
```